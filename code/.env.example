# Copy this file to code/.env and fill in the value.
# code/.env is git-ignored and must never be committed.
#
# A real environment variable always takes precedence over this file, so
# `export ANTHROPIC_API_KEY=...` overrides whatever is set here.

ANTHROPIC_API_KEY=
